#include<stdio.h>
main(){

    float input_1,input_2;
     //asking the user to provide two floating point numbers
     printf("Enter any two floating point numbers :");

    //taking the two floating point numbers as input from the user
      scanf("%f%f",&input_1,&input_2);

    //showing the result of multiplication of these two floating point numbers
     printf("The multiplication of these two floating points is : %f",input_1*input_2);


    }

